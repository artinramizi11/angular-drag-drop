import { Routes } from '@angular/router';
import { DragDropComponent } from './drag-drop/drag-drop.component';

export const routes: Routes = [
    {
        path:"",
        component: DragDropComponent,
        title:"Note Drag-Drop App"
    }
];
